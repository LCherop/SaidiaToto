<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laura.saidiatoto">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_cherry"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_cherry_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaidiaToto">
        <activity android:name=".FeverActivity"></activity>
        <activity android:name=".VomitActivity" />
        <activity android:name=".NosebleedActivity" />
        <activity android:name=".HeadInjuryActivity" />
        <activity android:name=".SubstanceActivity" />
        <activity android:name=".CroupActivity" />
        <activity android:name=".ChokingChildActivity" />
        <activity android:name=".ChokingBabyActivity" />
        <activity android:name=".BurnActivity" />
        <activity android:name=".FractureActivity" />
        <activity android:name=".TitleViewer" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".activity_settings_page" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Profile" />
        <activity android:name=".OTP_number" />
        <activity android:name=".Login" />
        <activity android:name=".Landing_Page" />
        <activity android:name=".SignUp" />
        <activity android:name=".Forgot_Password" />
        <activity android:name=".NearbyHospitals" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>